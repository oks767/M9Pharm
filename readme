Тестовое задание для М9 Фарм.
Чтобы запустить проект необходимо выполнить команды:

npm install
npm start

после выполнения этих команд запуститься сверстанный проект. В проекте можно свернуть контент, за это отвечает файл accordeon.js, удалить строку (файл removeStringTable.js) - строка удаляется, а следующая сразу поднимается на место удаленной, чтобы это было видно, я изменила последнюю цифру в названии элемента соответсвенно строке, в первых четырех строках. Также можно добавить строку по клику на "добавить элемент" (файл addStringTable.js)